#!/usr/bin/env bash
# Run a command inside the Python 3.14 test container.
#
# Home Assistant tracks the newest Python closely, so its test framework cannot be
# installed on an older interpreter. Rather than add one to the host, everything runs in a
# container. The arcam_fmj fork checkout next door is put on PYTHONPATH instead of being
# pip-installed, so the integration is always tested against the JBL branch source and no
# build is needed from the read-only mount.
#
#   .devtools/run pytest
#   .devtools/run ruff check .
#   .devtools/run python -c 'import arcam.fmj'
#
set -euo pipefail

here="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
lib="$(cd "$here/../arcam_fmj" && pwd)"

if ! docker image inspect ha-jblsynthesis-test >/dev/null 2>&1; then
  echo "building test image..." >&2
  docker build -q --platform linux/amd64 -f "$here/.devtools/Dockerfile" -t ha-jblsynthesis-test "$here" >&2
fi

exec docker run --rm --platform linux/amd64 \
  -e PYTHONPATH=/opt/arcam_fmj/src \
  -e PYTHONDONTWRITEBYTECODE=1 \
  -v "$here":/work \
  -v "$lib":/opt/arcam_fmj:ro \
  -w /work \
  ha-jblsynthesis-test "$@"
